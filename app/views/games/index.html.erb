<%= render 'shared/header' %>
<table class="table table-hover table-striped table-condensed">
  <thead>
    <tr>
      <th>Games List</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @games.each do |game| %>
      <tr>
        <td><%= link_to game.title, game %></span></td>
        <br>
        <td><%= link_to 'Rename', edit_game_path(game), class: "btn btn-default btn-xs"  %></td>
        <td><%= link_to 'Delete', game, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-default btn-xs"  %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Add a Game', new_game_path, class: "btn btn-default btn-xs"  %>
<%= render 'shared/donate' %>
